import { KaraokeChunkDirective } from './karaoke-chunk.directive';

describe('KaraokeChunkDirective', () => {
  it('should create an instance', () => {
    const directive = new KaraokeChunkDirective();
    expect(directive).toBeTruthy();
  });
});
